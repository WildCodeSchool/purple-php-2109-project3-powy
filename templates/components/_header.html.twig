<header class="header-index">
    <div class="logo">
        <dic class="logo-img">
            <a href="{{ path("home") }}"><h1>Powy</h1></a>
            <img src="{{ asset('build/images/logo.png') }}" alt="logo">
        </dic>
        <p>Le pouvoir de transmettre</p>
    </div>
    <div class="connexion-btn">
        {% if app.user %}
            <a href="{{ path('profile_index') }}"><img src="{{ asset('build/images/silhouette-connexion.png') }}" alt="connexion"></a>
            <div>
                <a href="{{ path('profile_index') }}">Mon compte</a>
                <a href="/logout">Déconnexion</a>
            </div>
        {% else %}
            <a href="/login"><img src="{{ asset('build/images/silhouette-connexion.png') }}" alt="connexion">Mon compte</a>
        {% endif %}
    </div>
</header>
