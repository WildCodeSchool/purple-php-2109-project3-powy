<form method="post" action="{{ path('profile_delete') }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer votre profil ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete') }}">
    <button class="btn-profile delete-btn">Supprimer</button>
</form>