{% extends 'base.html.twig' %}

{% block title %}Inscription étudiant{% endblock %}

{% block body %}
    {% include "components/_header.html.twig" %}
    {% include "components/_flashmessages.html.twig" %}
    <h1>Formulaire d'inscription étudiant</h1>

    {{ form_start(studentRegistrationForm) }}
        {{ form_errors(studentRegistrationForm) }}
        <small>Sélectionne la case correspondant à ta situation.</small>
        {{ form_row(studentRegistrationForm.studyLevel, {
            label: 'Quel est ton niveau actuel ?'
        }) }}
        <hr>
        <p>Sur quels points souhaites-tu t'améliorer ?</p>
        <small>Sélectionne <strong>3 sujets</strong> dans l'ordre d'importance.</small>
        {{ form_row(studentRegistrationForm.topics.topic1, {
            label: 'choix 1'
        }) }}
        {{ form_row(studentRegistrationForm.topics.topic2, {
            label: 'choix 2'
        }) }}
        {{ form_row(studentRegistrationForm.topics.topic3, {
            label: 'choix 3'
        }) }}
        <hr>
        <p>On voudrait en savoir plus sur toi pour mieux t'accompagner.</p>
        {{ form_row(studentRegistrationForm.school, {
            label: 'Renseigne ton établissement scolaire :'
        }) }}
        {{ form_row(studentRegistrationForm.professionalSector, {
            label: 'Secteur professionnel idéal'
        }) }}
        {{ form_row(studentRegistrationForm.dreamJob, {
            label: 'Ton rêve professionnel :'
        }) }}
        {{ form_row(studentRegistrationForm.dreamDescription, {
            label: 'Précise en quelques mots :'
        }) }}
        <hr>
        {{ form_row(studentRegistrationForm.scholarship, {
            label: 'Es-tu boursier ?'
        }) }}
        <hr>
        <p>Qui es-tu ?</p>
        <small>Ces informations seront communiquées à tes futurs mentors.</small>
        {{ form_row(studentRegistrationForm.user.firstname, {
            label: 'Prénom'
        }) }}
        {{ form_row(studentRegistrationForm.user.lastname, {
            label: 'Nom'
        }) }}
        {{ form_row(studentRegistrationForm.user.gender, {
            label: 'Genre'
        }) }}
        {{ form_row(studentRegistrationForm.user.age, {
            label: 'Âge'
        }) }}
        {{ form_row(studentRegistrationForm.user.phone, {
            label: 'Téléphone'
        }) }}
        <hr>
        <p>Dernière étape : il te suffit de rentrer tes informations de connexion et c'est fini !</p>
        {{ form_row(studentRegistrationForm.user.email) }}
        {{ form_row(studentRegistrationForm.plainPassword, {
            label: 'Mot de passe'
        }) }}
        {{ form_widget(studentRegistrationForm.user.agreeTerms) }}
        {{ form_label(studentRegistrationForm.user.agreeTerms, 'J\'accepte les CGU ainsi que le fait que mes données soient utilisées dans le cadre de la politique de confidentialité.') }}
        <button type="submit" class="btn">s'inscrire</button>
    {{ form_end(studentRegistrationForm) }}
{% endblock %}
